apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: journee-d3b7bc
  namespace: journee
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: letsencrypt-cloudflare
spec:
  ingressClassName: "cloudflare-tunnel"
  rules:
    - host: journee-d3b7bc.aooba.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: journee-d3b7bc
                port:
                  number: 80
