apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: journee-e754eb
  namespace: journee
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: letsencrypt-cloudflare
spec:
  ingressClassName: "cloudflare-tunnel"
  rules:
    - host: journee-e754eb.aooba.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: journee-e754eb
                port:
                  number: 80
