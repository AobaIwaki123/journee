apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: journee-a051e4
  namespace: journee
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: letsencrypt-cloudflare
spec:
  ingressClassName: "cloudflare-tunnel"
  rules:
    - host: journee-a051e4.aooba.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: journee-a051e4
                port:
                  number: 80
