name: Playwright E2E Tests

on:
  pull_request:

# PRへの書き込み権限を付与
permissions:
  contents: write
  pull-requests: write
  checks: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  e2e:
    runs-on: ubuntu-latest
    env:
      NEXTAUTH_URL: http://localhost:3000
      NEXTAUTH_SECRET: ${{ secrets.NEXTAUTH_SECRET }}
      GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
      GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      ENCRYPTION_KEY: ${{ secrets.ENCRYPTION_KEY }}
      NEXT_PUBLIC_ENABLE_MOCK_AUTH: true
      PLAYWRIGHT_BROWSERS: chromium # ここでブラウザを指定 (複数可: chromium,firefox,webkit)      
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest  # または特定のバージョン (例: '1.1.34')
          
      - name: Install dependencies
        run: bun install --frozen-lockfile
      
      - name: Get Playwright version
        id: playwright-version
        run: |
          VERSION=$(bunx playwright --version | grep -oP 'Version \K[0-9.]+')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
      
      - name: Cache Playwright browsers
        uses: actions/cache@v5
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: playwright-browsers-${{ runner.os }}-${{ steps.playwright-version.outputs.version }}-${{ env.PLAYWRIGHT_BROWSERS }}    
      
      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: bunx playwright install --with-deps ${{ env.PLAYWRIGHT_BROWSERS }}
      
      - name: Install Playwright system dependencies
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: bunx playwright install-deps ${{ env.PLAYWRIGHT_BROWSERS }}
        
      - name: Run E2E tests
        id: playwright-tests
        continue-on-error: true
        run: bunx playwright test
      
      # GitHub Summaryに結果を出力
      - name: Generate test summary
        if: always()
        run: |
          echo "## 🎭 Playwright E2E Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ -f playwright-report/index.html ]; then
            # テスト結果の統計を抽出（簡易版）
            if [ "${{ steps.playwright-tests.outcome }}" == "success" ]; then
              echo "✅ **All tests passed!**" >> $GITHUB_STEP_SUMMARY
            else
              echo "❌ **Some tests failed**" >> $GITHUB_STEP_SUMMARY
            fi
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "📊 [View detailed report in Artifacts](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ No test report generated" >> $GITHUB_STEP_SUMMARY
          fi
          
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
  
  # ここからレポートpush処理
  # 参考: https://zenn.dev/leaner_dev/articles/88272c891d4fb5
  publish_report:
    name: Publish Report
    # always() だとワークフローをキャンセルした場合も実行されてしまうので、明示的に指定する
    if: "success() || needs.e2e.result == 'failure'"
    needs: [e2e]
    runs-on: ubuntu-latest
    continue-on-error: true
    env:
      # ワークフロー実行ごとに一意となるパスを組み立てる
      HTML_REPORT_URL_PATH: reports/${{ github.ref_name }}/${{ github.run_id }}/${{ github.run_attempt }}
    steps:
      - name: Checkout GitHub Pages Branch
        uses: actions/checkout@v4
        with:
          ref: gh-pages

      - name: Set Git User
        # GitHub Actions botとしてレポート追加のコミットを作成する
        # See: https://github.com/orgs/community/discussions/26560
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Download zipped HTML report
        uses: actions/download-artifact@v4
        with:
          name: playwright-report
          path: ${{ env.HTML_REPORT_URL_PATH }}

      - name: Push HTML Report
        timeout-minutes: 3
        # gh-pagesブランチにレポート追加のコミットをpushできるまでrebase, pushを繰り返す
        # 異なるPRで同時にCIが走った場合など、このジョブが同時に実行される場合でも全てのレポートをpushするために必要
        run: |
          git add .
          git commit -m "workflow: add HTML report for run-id ${{ github.run_id }} (attempt:  ${{ github.run_attempt }})"

          while true; do
            git pull --rebase
            if [ $? -ne 0 ]; then
              echo "Failed to rebase. Please review manually."
              exit 1
            fi

            git push
            if [ $? -eq 0 ]; then
              echo "Successfully pushed HTML report to repo."
              exit 0
            fi
          done

      - name: Output Report URL as Workflow Annotation and PR Comment
        if: github.event_name == 'pull_request'
        run: |
          FULL_HTML_REPORT_URL=https://${{ github.repository_owner }}.github.io/$(echo ${{ github.repository }} | cut -d'/' -f2)/$HTML_REPORT_URL_PATH

          echo "::notice title=📋 Published Playwright Test Report::$FULL_HTML_REPORT_URL"
          
          # PRコメントを追加
          gh pr comment ${{ github.event.pull_request.number }} \
            --body "## 📋 Playwright Test Report
          
          Test report has been published: $FULL_HTML_REPORT_URL"
        env:
          GH_TOKEN: ${{ github.token }}